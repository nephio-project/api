---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: servedguamis.cellular.config.nephio.org
spec:
  group: cellular.config.nephio.org
  names:
    kind: ServedGUAMI
    listKind: ServedGUAMIList
    plural: servedguamis
    singular: servedguami
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ServedGUAMISpec defines the servedGUAMI for an AMF
            properties:
              amfID:
                description: AMFID defines the AMF Identifier
                properties:
                  amfPointer:
                    description: AMFPointer uniquely identifies the AMF in AMF set
                    maxLength: 6
                    pattern: '[01]*'
                    type: string
                  amfRegionID:
                    description: AMFRegionID identifies the region
                    maxLength: 8
                    pattern: '[01]*'
                    type: string
                  amfSetID:
                    description: AMFfSetID uniquely identifies the AMF Set within
                      the AMF Region
                    maxLength: 8
                    pattern: '[01]*'
                    type: string
                required:
                - amfPointer
                - amfRegionID
                - amfSetID
                type: object
              plmnID:
                description: PlmnID defines the PLMN Identifier
                properties:
                  mcc:
                    description: mcc defines the mobile country code
                    pattern: '[02-79][0-9][0-9]'
                    type: string
                  mnc:
                    description: mnc defines the mobile network code
                    pattern: '[0-9][0-9][0-9]|[0-9][0-9]'
                    type: string
                required:
                - mcc
                - mnc
                type: object
            required:
            - amfID
            - plmnID
            type: object
          status:
            description: ServedGUAMIStatus defines the observed state of ServedGUAMI
            type: object
        type: object
    served: true
    storage: true
