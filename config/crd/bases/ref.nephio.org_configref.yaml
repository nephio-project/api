---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: configref.ref.nephio.org
spec:
  group: ref.nephio.org
  names:
    kind: ConfigRef
    listKind: ConfigRefList
    plural: configref
    singular: configref
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConfigRef is the Schema for the ConfigRef API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ConfigRefSpec defines the structure for config reference
              specification
            properties:
              gvkc:
                description: GVK specifies the referenced data embedded inside ConfigRef
                properties:
                  config:
                    description: Config specifies the resource content (spec) in JSON
                      string format for this ref
                    type: string
                  group:
                    description: Group specifies the Kubernetes API group (api version)
                      for this ref
                    type: string
                  kind:
                    description: Kind specifies the resource kind for this ref
                    type: string
                  version:
                    description: Version specifies the version for this ref
                    type: string
                type: object
            type: object
          status:
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
